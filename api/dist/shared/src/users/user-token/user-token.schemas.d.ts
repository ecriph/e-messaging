import { Schema } from 'zod';
import { UserAuthTokenDTO, UserTokenDTO } from './user-token.dto';
export declare const UserTokenSchema: Schema<UserTokenDTO>;
export declare const UserAuthTokenSchema: Schema<UserAuthTokenDTO>;
